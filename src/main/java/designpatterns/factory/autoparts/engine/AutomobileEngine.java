package designpatterns.factory.autoparts.engine;

import designpatterns.factory.autoparts.AutomobilePart;

public class AutomobileEngine extends AutomobilePart {
    public AutomobileEngine(String description) {
        super(description);
    }
}
